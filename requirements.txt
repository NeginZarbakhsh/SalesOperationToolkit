# what the toolkit and the dashboard need
pandas>=2.2
numpy>=1.26
matplotlib>=3.8      # charts written to outputs/
openpyxl>=3.1        # Excel workbook export
streamlit>=1.40      # the dashboard (app.py)
altair>=5.0          # dashboard charts
pillow>=10.0         # dashboard icon
anthropic>=1.0       # optional: the AI summary and Ask AI; everything else works without a key
